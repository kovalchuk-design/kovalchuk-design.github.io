---
version: 1
slug: "index-v3-html"
primary_target: "index-v3.html"
related_targets: []
---

# Surface: index-v3.html (deep redesign)

Scope: whole-homepage visual world replacement, code-led (no image generation available). Mode: Persuade — visitor's job is deciding whether to contact Serhii, not just admiring work. Color tokens are pinned (brand commitment): `--bg #0D0F12`, `--bg-lift #14171C`, `--ink #E9EAE5`, `--ink-2 #9AA1A9`, `--ink-3 #8A9199`, `--accent #1B3FD8`, `--accent-lift #7C97FF`. Content, facts, metrics, and links into `work/*.html` are fixed (PRODUCT.md). Typography, layout, composition, and component language are open.

## Direction contract

THESIS: The homepage is not an article you scroll, it's an instrument you monitor. Refuses the category default this exact site already used: hero statement → card grid → footer CTA editorial scroll.

OWN-WORLD: Same pinned color tokens, redeployed as a Committed strategy — the accent is a "target-lock" signal (active nav, hovered case, the sweep line), not just a link-hover tint. A persistent scope frame (corner brackets, coordinate/status readout, range-rings) stays on screen while content scrolls inside it like radar returns. JetBrains Mono is elevated from "label font" to the primary information-carrying face; Archivo stays for the one identity mark. Case studies read as tracked contacts (heading/status metadata) instead of article cards.

STORY: The visitor immediately reads "monitoring high-stakes traffic" — signaling this designer works where nothing can be missed. They scan the scope for contacts (RxLogix, NewDay, Carnival) as tracked items, read the metrics as live instrument readouts, and treat contact as "opening a channel."

FIRST VIEWPORT: A corner-bracket scope frame at the viewport edge (HUD-like), persistent through the whole page. Left: the name as a call-sign readout with a range-ring emblem. Right: a live status ticker (current section, "OPEN TO WORK" flag, build/session-style stamp). The lede runs as a contact-log line inside the frame. The proof strip becomes a 4-up instrument cluster: dial-style numbers, tabular-nums, range-ring dividers instead of a plain CSS grid. Primary actions are labeled frequency-selector buttons, always reachable at the bottom-left of the frame.

FORM: Air-traffic-control / radar-scope monitoring display. Candidate 4 of 7 grounded directions ordered by resonance (1. aviation checklist/procedure card, 2. engineering redline/blueprint markup, 3. financial audit workpaper, 4. ATC scope [assigned], 5. clinical case-report form, 6. pharmacopoeia insert, 7. bank ledger/seal). Seed key `cdff33b3`, degraded roll (no network egress to the challenger catalog, disclosed to the user), direction confirmed by the user via the structured question tool over the assigned card, the pick card (aviation checklist), and a re-roll option.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.
